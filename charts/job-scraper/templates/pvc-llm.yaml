apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: job-scraper-llm-cache
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "job-scraper.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.pvc.llm.storageClass }}
  resources:
    requests:
      storage: {{ .Values.pvc.llm.size }}
